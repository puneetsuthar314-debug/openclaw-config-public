# Agent 操作日志 - 2026-03-17 模型路由全流程验证

## 任务概述
验证 OpenClaw 8 模型路由方案是否能真实落地运行，模拟钉钉用户发送不同类型任务，检查 GLM-5 是否按框架调度专家模型。

## 关键操作
1. 通过 DashScope API 逐一测试 8 个模型的可用性 - 全部通过
2. 通过 openclaw agent CLI 模拟钉钉消息 - 发现 GLM-5 不会自动切换模型
3. 根因分析：model-routing.md 不在 system prompt 注入列表中
4. 将路由规则核心内容嵌入 AGENTS.md（从 852 chars 到 2600 chars）
5. 重启 Gateway，用真实钉钉 session 重新测试
6. 4 个场景全部通过：编码到coder，推理到qwen3-max，日常GLM-5直接处理，写作到MiniMax

## 修改文件
- AGENTS.md - 嵌入模型路由规则（备份: AGENTS.md.bak.202603171621）
- openclaw.json - 默认模型改为 glm-5（备份: openclaw.json.bak.pre-glm5）

## 风险与注意
- 长文写作任务可能超时（MiniMax-M2.5 生成 3000 字需要超过60s）
- Kimi K2.5 视觉路由未在真实图片场景中验证
- CLI 临时 session 不支持 session_status 切换，仅持久化 session 可用
