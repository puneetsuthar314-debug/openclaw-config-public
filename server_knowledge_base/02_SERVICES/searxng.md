> **核心洞察**：这是一个**已废弃**的服务。虽然配置文件存在，但服务从未被配置为自动运行。可以安全地忽略它。

# 02 - 服务：SearXNG (已废弃)

**最后更新**: `2026-03-17`

## 1. 状态

- **状态**: **已配置但离线**。
- **配置文件**: 位于 `/opt/searxng/`。
- **结论**: 这是一个历史遗留的软件，当前服务器上没有任何服务依赖它，也没有配置任何自动启动机制。

## 2. 操作建议

- **不要启动它**：除非有明确的用户请求，否则不要尝试启动或配置此服务。
- **可以清理**：如果未来磁盘空间极度紧张，`/opt/searxng/` 目录可以作为一个清理候选项，但在删除前需要再次确认没有新的变更依赖它。
