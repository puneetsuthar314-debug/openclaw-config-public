> **说明**: 当您在服务器上部署一个新服务或发现一个未被记录的服务时，请复制此模板，并以服务名称命名（例如 `MyNewService.md`），然后填写所有字段。

# 02 - 服务：[服务名称]

**最后更新**: `YYYY-MM-DD`

> **核心洞察**: (用一句话总结这个服务的管理方式、核心特点或风险。)

## 1. 核心机制与管理方式

(解释这个服务是如何启动和管理的。是通过 `systemd`，`cron`，还是手动启动？是否存在多个管理方式的冲突？)

> **操作雷区**: (列出在管理此服务时最需要注意的“坑”或风险点。)

## 2. 如何正确管理服务

(提供一个清晰的、包含首选命令的表格，指导 Agent 如何安全地进行启停、查看状态和日志等操作。)

| 任务 | 首选命令 | 备注 |
|---|---|---|
| **重启服务** | `systemctl restart my-service` | |
| **停止服务** | `systemctl stop my-service` | |
| **查看状态** | `systemctl status my-service` | |
| **查看日志** | `journalctl -u my-service -f` | |

## 3. 故障排查指南

- **如果服务无法启动**: 
  1.  (第一步该检查什么？例如 `journalctl` 的输出)
  2.  (第二步该检查什么？例如配置文件语法)

## 4. 关联性

- **依赖的服务**: (例如: 需要 PostgreSQL 数据库、Redis 缓存)
- **被谁依赖**: (例如: 是被 Nginx 反向代理，还是被其他服务调用)
